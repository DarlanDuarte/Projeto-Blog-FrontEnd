import React from "react";

const Footer: React.FC = () => {
  return (
    <footer className={`w-screen h-10 bg-white mt-4 `}>
      <div className={`text-center p-2`}> Footer &copy; 2023 </div>
    </footer>
  );
};

export default Footer;
